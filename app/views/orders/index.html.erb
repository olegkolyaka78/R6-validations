<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table>
  <thead>
    <tr>
      <th>Order ID</th>
      <th>Product name</th>
      <th>Product count</th>
      <th>Customer</th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.product_name %></td>
        <td><%= order.product_count %></td>
        <td><%= order.customer.full_name %></td>
        <td><%= link_to 'Show', order, class: "btn btn-primary"  %></td>
        <td><%= link_to 'Edit', edit_order_path(order), class: "btn btn-primary"  %></td>
        <td><%= link_to 'Delete', order, class: "btn btn-primary",  method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_order_path %> | <%= link_to 'Customers', customers_path %>

